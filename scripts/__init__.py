# Scripts package — evaluation, visualization, and dataset utilities.
# Add the repo root to sys.path so these scripts can import core modules.
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))
